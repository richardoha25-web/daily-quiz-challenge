import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId:'com.richard.dailyquizchallenge', appName:'Daily Quiz & Challenge', webDir:'dist', bundledWebRuntime:false };
export default config;
